using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "NetManager", menuName = "NetManager")]
public class ScriptableNetManager : ScriptableObject {

    //Now this script is only doing the container int, what can we change here
    public int currentContainer;
}
